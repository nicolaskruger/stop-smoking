import styled from "styled-components";

const H1 = styled.h1`
    color: ${props => props.theme.textColor01};
`;
const P = styled.p`
    color: ${props => props.theme.textColor01};
`;

export {
    H1,
    P
}